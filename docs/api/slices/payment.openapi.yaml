openapi: 3.1.0
info:
  title: Greenhouse API Slice - payment
  version: 0.0.1
servers:
- url: http://localhost:8080
  description: Generated server url
paths:
  /api/auction-settlements/{settlementId}/confirm-payment:
    post:
      tags:
      - payment-controller
      operationId: confirmAuctionPayment
      parameters:
      - name: settlementId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ManualPaymentRequest'
        required: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponseAuctionSettlementResponse'
  /api/partner-payment-events:
    get:
      tags:
      - payment-controller
      description: 호환용 목록. 입금일·ID 역순으로 최신 500건까지 반환합니다. 운영 화면은 /partner-payment-events/page를 사용합니다.
      operationId: getEvents
      parameters:
      - name: partnerId
        in: query
        required: false
        schema:
          type: integer
          format: int64
      - name: targetType
        in: query
        required: false
        schema:
          type: string
          enum:
          - SALES_SLIP
          - AUCTION_SETTLEMENT
          - NONE
      - name: targetId
        in: query
        required: false
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponseListPartnerPaymentEventResponse'
      deprecated: true
  /api/partner-payment-events/page:
    get:
      tags:
      - payment-controller
      description: 조건에 맞는 입금 이벤트를 입금일·ID 역순으로 조회합니다. 유형 필터는 페이지 조회 전 적용됩니다. page는 0 이상, size는 1~100으로 보정합니다.
      operationId: getPaymentEventPage
      parameters:
      - name: partnerId
        in: query
        required: false
        schema:
          type: integer
          format: int64
      - name: targetType
        in: query
        required: false
        schema:
          type: string
          enum:
          - SALES_SLIP
          - AUCTION_SETTLEMENT
          - NONE
      - name: targetId
        in: query
        required: false
        schema:
          type: integer
          format: int64
      - name: eventType
        in: query
        required: false
        schema:
          type: string
          enum:
          - PAYMENT_RECEIVED
          - PAYMENT_ALLOCATED
          - PREPAYMENT_RECEIVED
          - CREDIT_APPLIED
          - CREDIT_REFUND
          - AUTO_MATCH_CANDIDATE
          - AUTO_MATCH_CONFIRMED
          - MANUAL_MATCH_CONFIRMED
          - MATCH_REJECTED
          - PAYMENT_UNLINKED
          - ADJUSTMENT
      - name: page
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 0
      - name: size
        in: query
        required: false
        schema:
          type: integer
          format: int32
          default: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponsePageResponsePartnerPaymentEventResponse'
  /api/business-partners/{partnerId}/balance-summary:
    get:
      tags:
      - payment-controller
      operationId: getBalance
      parameters:
      - name: partnerId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponsePartnerBalanceSummaryResponse'
components:
  schemas:
    ApiResponseAuctionSettlementResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/AuctionSettlementResponse'
        message:
          type: string
    ApiResponseListPartnerPaymentEventResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/PartnerPaymentEventResponse'
        message:
          type: string
    ApiResponsePageResponsePartnerPaymentEventResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/PageResponsePartnerPaymentEventResponse'
        message:
          type: string
    ApiResponsePartnerBalanceSummaryResponse:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/PartnerBalanceSummaryResponse'
        message:
          type: string
    AuctionSettlementLineResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        auctionResultLineId:
          type: integer
          format: int64
        auctionShipmentLotId:
          type: integer
          format: int64
        shipmentDate:
          type: string
          format: date
        varietyName:
          type: string
        shipmentGrade:
          type: string
        quantity:
          type: integer
          format: int32
        unitPrice:
          type: integer
          format: int32
        amount:
          type: integer
          format: int64
        status:
          type: string
          enum:
          - UNPAID
          - PAID
          - PARTIALLY_PAID
          - EXCLUDED
          - REVIEW_REQUIRED
    AuctionSettlementResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        auctionHouseId:
          type: integer
          format: int64
        auctionHouseName:
          type: string
        auctionDate:
          type: string
          format: date
        resultReceivedAt:
          type: string
          format: date-time
        expectedPaymentDate:
          type: string
          format: date
        grossAmount:
          type: integer
          format: int64
        feeAmount:
          type: integer
          format: int64
        deductionAmount:
          type: integer
          format: int64
        expectedDepositAmount:
          type: integer
          format: int64
        paidAmount:
          type: integer
          format: int64
        remainingAmount:
          type: integer
          format: int64
        status:
          type: string
          enum:
          - CREATED
          - PAYMENT_WAITING
          - PARTIALLY_PAID
          - PAID
          - AMOUNT_MISMATCH
          - REVIEW_REQUIRED
          - CANCELLED
        memo:
          type: string
        confirmedAt:
          type: string
          format: date-time
        confirmedBy:
          type: string
        lines:
          type: array
          items:
            $ref: '#/components/schemas/AuctionSettlementLineResponse'
    ManualPaymentRequest:
      type: object
      properties:
        amount:
          type: integer
          format: int64
        paymentDate:
          type: string
          format: date
        idempotencyKey:
          type: string
          maxLength: 100
          minLength: 0
        paymentMethod:
          type: string
          maxLength: 30
          minLength: 0
        depositorName:
          type: string
          maxLength: 100
          minLength: 0
        worker:
          type: string
          maxLength: 100
          minLength: 0
        memo:
          type: string
          maxLength: 1000
          minLength: 0
      required:
      - amount
      - idempotencyKey
      - paymentDate
    PageResponsePartnerPaymentEventResponse:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/PartnerPaymentEventResponse'
        page:
          type: integer
          format: int32
        size:
          type: integer
          format: int32
        totalElements:
          type: integer
          format: int64
        totalPages:
          type: integer
          format: int32
    PartnerBalanceSummaryResponse:
      type: object
      properties:
        partnerId:
          type: integer
          format: int64
        partnerName:
          type: string
        creditBalance:
          type: integer
          format: int64
        unappliedPaymentAmount:
          type: integer
          format: int64
        receivableBalance:
          type: integer
          format: int64
    PartnerPaymentEventResponse:
      type: object
      properties:
        id:
          type: integer
          format: int64
        partnerId:
          type: integer
          format: int64
        partnerName:
          type: string
        eventType:
          type: string
          enum:
          - PAYMENT_RECEIVED
          - PAYMENT_ALLOCATED
          - PREPAYMENT_RECEIVED
          - CREDIT_APPLIED
          - CREDIT_REFUND
          - AUTO_MATCH_CANDIDATE
          - AUTO_MATCH_CONFIRMED
          - MANUAL_MATCH_CONFIRMED
          - MATCH_REJECTED
          - PAYMENT_UNLINKED
          - ADJUSTMENT
        eventDate:
          type: string
          format: date
        amount:
          type: integer
          format: int64
        unappliedAmount:
          type: integer
          format: int64
        targetType:
          type: string
          enum:
          - SALES_SLIP
          - AUCTION_SETTLEMENT
          - NONE
        targetId:
          type: integer
          format: int64
        parentEventId:
          type: integer
          format: int64
        paymentMethod:
          type: string
        depositorName:
          type: string
        description:
          type: string
        status:
          type: string
          enum:
          - UNAPPLIED
          - PARTIALLY_APPLIED
          - FULLY_APPLIED
          - CANDIDATE
          - CONFIRMED
          - REJECTED
          - CANCELLED
        memo:
          type: string
        createdBy:
          type: string
