apiVersion: v1
kind: Service
metadata:
  name: green-house-backend
  namespace: green-house
  labels:
    app.kubernetes.io/name: green-house
    app.kubernetes.io/component: backend
spec:
  selector:
    app.kubernetes.io/name: green-house
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 8080
      targetPort: http
